<app-page id="notes" title="Notes">
  <template slot="bar-left">
    <button v-on:click="app.toPage(homePage)" v-if="homePage && (app.page !== homePage)"><i class="fas fa-home"></i></button>
    <button v-on:click="app.toPage('notes', dirname())" v-if="path" title="Up"><i class="fas fa-chevron-up"></i></button>
  </template>
  <template slot="bar-right">
    <button v-if="path !== ''" v-on:click="onDelete().then(function(){ app.back(); })" title="Delete"><i class="fa fa-trash"></i></button>
    <button v-on:click="createNote()" title="Create new note"><i class="fa fa-plus"></i></button>
    <button v-on:click="onRefresh()" title="Refresh"><i class="fas fa-sync"></i></button>
  </template>
  <article class="tiles">
    <div class="tile" v-on:click="openNote(note)" v-for="note in notes">
      <p>{{ note.name }}</p>
      <p class="tile-value">
        <i v-if="note.type === 'dir'" class="fas fa-folder"></i>
        <i v-else-if="note.type === 'text'" class="fas fa-file-alt"></i>
        <i v-else-if="note.type === 'draw'" class="fas fa-file-image"></i>
        <i v-else-if="note.type === 'link'" class="fas fa-link"></i>
        <i v-else class="fas fa-file"></i>
      </p>
    </div>
  </article>
</app-page>