{
  "name": "Web Tools",
  "description": "Expose additional web tools",
  "dependencies": ["web-base"],
  "version": "1.0",
  "script": "web-tools.lua",
  "schema": {
    "type": "object",
    "properties": {
      "log": {
        "title": "Enables log level filter",
        "description": "Increase the log level for an HTTP request by using the header jls-logger-level",
        "type": "boolean",
        "default": false
      }
    }
  }
}