{
  "name": "Ping",
  "description": "Test the reachability of a host on the network",
  "version": "1.0",
  "script": "ping.lua",
  "dependencies": ["execute"],
  "schema": {
    "type": "object",
    "properties": {
      "target_names": {
        "title": "Target IP address or host name",
        "type": "array",
        "items": {
          "type": "string"
        }
      }
    }
  }
}